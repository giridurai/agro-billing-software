import { RupeeToWordsPipe } from './rupee-to-words-pipe';

describe('RupeeToWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new RupeeToWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
